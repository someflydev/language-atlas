You are an expert technical writer and computing historian. Your task is to establish the core narrative documentation for the "language-atlas" project.

CONTEXT:
The "language-atlas" is a structured system mapping the history of programming languages. Markdown is treated as the canonical narrative layer of the project. We are capturing the soul, the history, and the tradeoffs of computing paradigms for future learners in the AI era.

TASK:
1. Create a `docs/` directory.
2. Create `docs/TIMELINE.md`: A grand narrative history of computing eras.
3. Create `docs/CONCEPTS.md`: Deep dives into persistent computing constructs.

CONSTRAINTS & REQUIREMENTS:
- `docs/TIMELINE.md` MUST cover the recurring "Software Crisis": COBOL vs C (business vs systems); Unix and Bell Labs; Lisp and symbolic computation; Structured programming (Pascal/Ada as a reaction to complexity); OOP explosion; Web explosion; the shift of Perl's role to Python; Ruby and developer happiness; JVM dominance / Big Data; Distributed systems & Cloud emergence; DevOps/IaC; and the modern systems correction (Rust/Go/Zig).
- `docs/CONCEPTS.md` MUST explain: Immutability, concurrency models (Erlang actors, CSP in Go, async/await), type systems (static/dynamic, nominal/structural), borrow checking, macros, and code-as-data.
- Write deeply and philosophically. Avoid generic encyclopedia tones. Explain *why* things happened, framing each era as a philosophical answer to a specific "Crisis" (e.g., complexity, concurrency, or scale).
- DO NOT embed Mermaid diagrams. Instead, if a visual would help, write a text block describing the diagram that *should* exist (e.g., "Diagram Required: An influence graph showing the flow of CSP from Hoare to Newsqueak to Go").
- Assume no prior files exist, just write the Markdown files from scratch.

COMMIT BEHAVIOR:
- Review the markdown for formatting and clarity.
- Commit changes to git.
- Commit message: "docs: author foundational narrative timeline and core concepts"
