You are a programming language historian. Your task is to expand the "language-atlas" dataset.

CONTEXT:
The "language-atlas" is a deeply structured system mapping programming language evolution. The schema and initial 15 languages (FORTRAN, Lisp, C, C++, Java, JavaScript, Python, Ruby, Haskell, Erlang, Go, Rust, TypeScript, SQL, Bash) are already established in `data/languages.json`. You must now add the first half of the remaining languages, focusing on Systems and Functional programming paradigms.

TASK:
1. Update `data/languages.json` to include the following 18 languages.

REQUIRED LANGUAGES TO ADD:
COBOL, Scheme, Pascal, Ada, Zig, Odin, Nim, Crystal, OCaml, F#, Clojure, Scala, Elixir, Perl, Lua, Julia, WebAssembly, LLVM.

CONSTRAINTS:
- CRITICAL: DO NOT OMIT any languages in this list.
- Maintain the exact keys for every entry in `data/languages.json`:
  `name`, `year`, `creators` (list), `paradigms` (list), `cluster` (e.g., systems, functional, frontend, scripting, data, infra), `generation` (early, web1, cloud, modern), `primary_use_cases` (string), `key_innovations` (string), `influenced_by` (list of names), `influenced` (list of names), `philosophy` (deep, non-generic string), `mental_model` (crucial: explain how the programmer must think), `complexity_bias` (low/medium/high), `safety_model` (manual/runtime/compile_time/hybrid).
- Ensure "influenced" and "influenced_by" relationships are cross-referenced correctly.
- Keep the depth of "philosophy" and "mental_model" high; avoid generic summaries.

COMMIT BEHAVIOR:
- Commit message: "feat: expand language atlas with systems and functional languages"
