You are an expert full-stack developer specializing in HTMX and Python. Your task is to build filtering and comparison features into the "language-atlas" Web UI.

CONTEXT:
The "language-atlas" has a foundational FastAPI + HTMX web UI. Users need the ability to filter the massive list of languages and conceptually compare them side-by-side, all without relying on heavy client-side JavaScript.

TASK:
1. Update `app.py` to add endpoints for filtering languages (by `cluster`, `generation`, or `paradigms`) and for comparing two languages. 
   - Note: Canonical generations in the data are: `dawn`, `early`, `web1`, `cloud`, `renaissance`, `autonomic`.
2. Update the Jinja2 templates to include a filtering sidebar or top-bar.
3. Update `static/css/styles.css` to handle the new layout requirements for comparison and filtering.
4. Create a new template `templates/compare.html` or a comparison partial that HTMX can swap into the DOM.

CONSTRAINTS & REQUIREMENTS:
- All filtering MUST be processed on the Python backend. The HTML form should use HTMX (e.g., `hx-get="/filter" hx-target="#language-list"`) to fetch updated HTML partials.
- The comparison feature should allow selecting two languages and viewing their philosophies, mental models, safety models, and paradigms side-by-side in a table or grid.
- Do NOT use React, Vue, or vanilla JS fetch API. Use strictly HTMX.
- Ensure the Python backend parses the `data/languages.json` correctly to perform these queries.

COMMIT BEHAVIOR:
- Verify that the HTMX attributes are correctly pointing to the FastAPI endpoints.
- Commit the changes to git.
- Commit message: "feat: implement htmx-driven filtering and side-by-side comparison"
