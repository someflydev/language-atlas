Objective: Clean up directory structures in `data/docs/` and prevent `scripts/dark_matter_audit.py` from scanning the `atlas_meta` files.

The highest quality data files in this repository are `data/languages.json`, `data/people.json`, `data/concepts.json`, `data/docs/language_profiles/`, and `data/docs/concept_profiles/`. The `data/docs/atlas_meta/` directory contains terms generated by LLMs, which we do not want to be scanned or audited as "dark matter".

Please complete the following steps:

1. **Update `scripts/dark_matter_audit.py` (Exclude `atlas_meta`)**:
   Modify the script so that it **never** scans the files within `data/docs/atlas_meta/` for references (dark matter). When the script iterates over `profiles_dirs` to read file contents and call `scan_profile_data`, ensure that the `atlas_meta` category is explicitly skipped.

2. **Move Specific JSON Files**:
   Several JSON files are unnecessarily nested in their own single-file directories. Move them directly to the `data/docs/` directory:
   - Move `data/docs/crossroads/crossroads.json` to `data/docs/crossroads.json`
   - Move `data/docs/timeline/timeline.json` to `data/docs/timeline.json`
   - Move `data/docs/modern_reactions/modern_reactions.json` to `data/docs/modern_reactions.json`

3. **Update Paths in Scripts**:
   Because you moved these files, you must update any hardcoded paths in the codebase that point to the old locations. For example, in `scripts/dark_matter_audit.py`, update the path for `crossroads.json` (e.g., change `docs_dir / "crossroads" / "crossroads.json"` to `docs_dir / "crossroads.json"`). Check other scripts as well if necessary.

4. **Delete Empty Directories**:
   Once the files are moved, remove their original, now-empty parent directories. **You must use `rmdir`** (do NOT use `rm -rf`):
   - `rmdir data/docs/crossroads`
   - `rmdir data/docs/timeline`
   - `rmdir data/docs/modern_reactions`

5. **Verify**:
   Run `python scripts/dark_matter_audit.py` to ensure the script executes successfully without errors, does not crash on missing files, and correctly skips the `atlas_meta` directory.