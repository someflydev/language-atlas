You are an expert Python web developer. Your task is to integrate Markdown rendering into the "language-atlas" Web UI.

CONTEXT:
The canonical narrative of the "language-atlas" lives in `.md` files (like `docs/TIMELINE.md`, `docs/LANGUAGE_PROFILES/*`). We need the FastAPI web UI to read, parse, and render these files beautifully into the HTML templates so users can read the history directly in the browser.

TASK:
1. Update `requirements.txt` to include a markdown parsing library (like `markdown` or `mistune`).
2. Update `app.py` to include endpoints that serve rendered Markdown content. For example, `/docs/{doc_name}` or `/language/{lang_name}/profile`.
3. Create a Jinja2 template `templates/markdown_view.html` to display the rendered HTML.

CONSTRAINTS & REQUIREMENTS:
- The backend must read the raw `.md` file, convert it to HTML safely, and pass it to the Jinja2 context using the `| safe` filter.
- Support rendering the `docs/LANGUAGE_PROFILES/` directory when a user clicks on a language's deep dive link.
- Update `static/css/styles.css` with a dedicated "markdown-body" section to make the Markdown content readable (e.g., styling for `h1`, `p`, `code`, `blockquote`).
- Handle 404s gracefully if a requested Markdown file does not exist.

COMMIT BEHAVIOR:
- Test the markdown to HTML pipeline conceptually.
- Commit the changes to git.
- Commit message: "feat: integrate server-side markdown rendering for narrative docs"
