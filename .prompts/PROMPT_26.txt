You are a senior Data Scientist and Language Historian. Your task is to perform an "Encyclopedic Expansion" of the Language Atlas, ensuring 100% accuracy and adding significant missing entries.

CONTEXT:
Our current dataset is a solid foundation but lacks breadth in "Pre-Dawn" languages, foundational computation concepts, and "Niche/Critical" modern systems. Additionally, our enums for `typing_discipline` and `memory_management` need standardization, and our concept profiles are missing.

TASK:
1.  **Deep Research & Correction (High Reasoning)**:
    *   Audit all entries in `data/languages.json`.
    *   **Accuracy Fix**: Ensure `typing_discipline` and `memory_management` use the standardized enum: `manual`, `runtime`, `compile_time`, `hybrid`. Move descriptive details (like "Strong/Static") into the `mental_model` or `philosophy` fields.
    *   **Data Consistency**: Ensure every language has accurate `creators` and `key_innovations` based on historical consensus.
2.  **Expansion (Adding 15+ Significant Languages & Foundations)**:
    *   Add at least 15 more significant entries, including:
        *   **Historical**: ALGOL 68, Modula-2, Oberon, Eiffel, Dylan, Tcl, PostScript, SETL.
        *   **Experimental/Niche**: Icon, K, J, Factor, Zig (check), Odin (check), Gleam.
        *   **Ancestors**: Plankalkül (first high-level), Short Code, Autocode.
    *   **Foundational Computation**: Add non-language foundations like "Machine Code", "Mathematical Notation", "Turing Machine", "Analytical Engine", "Von Neumann Architecture", "Boolean Algebra", and "Information Theory".
    *   For each new entry, provide a complete, high-quality profile JSON in `data/docs/language_profiles/`.
3.  **Comprehensive Profile System**:
    *   Create detailed profiles for at least 10 core concepts (e.g., The Actor Model, Borrow Checking, Design by Contract, Church-Turing Thesis) in a new directory: `data/docs/concept_profiles/`.
    *   Ensure each profile includes: `title`, `overview`, `historical_context`, `mental_model`, `key_aspects`, `tradeoffs`, `legacy`, and `ai_assisted_discovery_missions`.
4.  **Metadata Audit (People & Paradigms)**:
    *   **People**: Comprehensive audit of all profiles to ensure every mentioned creator/contributor is represented in `data/people.json` with their key contributions.
    *   **Paradigms**: Add missing paradigms (e.g., Set-theoretic, Goal-directed, Stack-based) used by new entries to `data/paradigms.json`.
5.  **Pipeline & SQLite Update**:
    *   Update `src/app/core/build_sqlite.py` to create `concept_profiles` and `concept_profile_sections` tables.
    *   Update `src/app/core/data_loader.py` to support loading and searching concept profiles.
    *   **Rebuild the Database**: Regenerate `language_atlas.sqlite` from the expanded JSON.
    *   **Re-run Validation**: Run `scripts/validate_data.py` to ensure the new data satisfies all integrity checks.
6.  **Full-Text Search (FTS) Optimization**:
    *   Implement `fts_concept_profiles` and update the global search view to index all narrative content from both language and concept profiles.

CONSTRAINTS:
- Accuracy is paramount. Use verified historical data.
- Update `AGENT.md` to mandate that all new languages and significant concepts MUST have detailed profiles.

COMMIT BEHAVIOR:
- Group changes into logical, multi-line commits (Tim Pope style).
- Commit message prefix: "[PROMPT_26]"
