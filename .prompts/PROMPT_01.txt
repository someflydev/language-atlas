You are an expert systems architect and programming language historian. Your task is to build the foundational data model and initial seed data for the "language-atlas" project.

CONTEXT:
The "language-atlas" is a deeply structured, explorable system that captures the historical evolution of programming languages, paradigms, and major computing inflection points. It relies on a canonical JSON data layer. This prompt focuses on establishing the schema and the first "Keystone" set of languages.

TASK:
1. Create a `data` directory.
2. Define the schema and comprehensive seed data for the following files:
   - `data/languages.json`
   - `data/paradigms.json`
   - `data/eras.json`
   - `data/influences.json`
   - `data/concepts.json`
   - `data/people.json`
   - `data/learning_paths.json` (Initialize as an empty list: `[]`)

CONSTRAINTS & REQUIREMENTS:
- `data/languages.json` MUST include the following 19 KEYSTONE languages in this first pass: 
  FORTRAN, ALGOL 60, Lisp, COBOL, SIMULA 67, C, Smalltalk, ML, C++, Java, JavaScript, Python, Ruby, Haskell, Erlang, Go, Rust, TypeScript, SQL, Bash.
- Each entry in `data/languages.json` MUST contain the exact keys:
  `name`, `year`, `creators` (list), `paradigms` (list), `cluster` (e.g., systems, frontend, scripting), `generation` (early, web1, cloud, modern), `primary_use_cases`, `key_innovations`, `influenced_by` (list), `influenced` (list), `philosophy` (deep, non-generic string), `mental_model` (crucial: explain how the programmer must think), `complexity_bias` (low/medium/high), `safety_model` (manual/runtime/compile_time/hybrid).
- All files must be flat, explicit JSON.
- Ensure the philosophy and mental models are historically accurate and philosophically rich.
- DO NOT use placeholders.

COMMIT BEHAVIOR:
- Commit message: "feat: establish canonical JSON schema and keystone language data"
